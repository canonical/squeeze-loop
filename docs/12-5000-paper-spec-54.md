# 12-5000-paper-spec-54 — paper-impl loop, circle 54: reframe the abstract (review2 #1/#2/#5)

STATUS: APPROVED
First circle acting on review2.md. The abstract is the reviewer's #1 surface: it
states the 15/15-vs-0/15 result unqualified, foregrounds skill accumulation, and
reads as if a comparison were done. Reframe per the user's confirmed decisions
(full reframe; skill -> mechanism demo; Hofstadter LEFT INTACT pending a decision).

## Changes (tex/paper.tex abstract)
1. Measurement + comparison sentences: relabel the barrier-on result as a wiring /
   consistency check; state explicitly this is a methods paper contributing the
   pattern + four existence demonstrations, NOT a comparative benchmark; mark the
   barrier-OFF contrast as a deterministic full-anchoring model (not a measurement)
   with the real-agent null adjacent (a real-agent pilot finds no difference at this
   scale); say the powered comparison against alternatives is deferred.
2. Generativity sentence: DEMOTE skill accumulation -- remove the "squeezed agents,
   whose skill bases enrich across cycles" clause; keep the paper's-own-construction
   generativity and add the solid reflexive empirical fact (gates caught
   \ResReflexDefects{} coherent-and-wrong defects in this manuscript before
   submission). Strengthens the honest core, drops the over-read claim.
3. Strange-loop closing sentence: UNCHANGED (scoped to structural form; Hofstadter
   body decision still open).

## Gates
- Gate B: build green; \ResReflexDefects (8) and \ResAll* are existing macros; no
  new \cite/number/macro/CLM; the abstract only weakens/relabels and restates
  body-SUPPORTED material (abstract-may-only-restate rule); refs resolve.
- Gate C: ledger unchanged (CITE 46 + RESULT 28); reflexive macros reconcile;
  reflexive re-run byte-identical.

## Note
This is the SAFE direction (claim less): the 15/15 becomes a consistency check, the
0 is labelled modeled-not-measured with the null beside it, skill is demoted, and
the paper is positioned as methods/pattern + existence demonstrations with the
comparison deferred. No empirical claim is strengthened.
