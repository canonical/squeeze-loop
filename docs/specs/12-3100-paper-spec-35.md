# 12-3100-paper-spec-35 — paper-impl loop, circle 35: polish the conclusion

STATUS: DONE
Editorial circle (no new evidence). Polish §sec:conclusion for readability.

## Change
The conclusion had grown into one ~25-line paragraph after circle 33 appended the
strange-loop beat. Split into three clean beats:
1. the strategy + the measured result + the honest eval scope;
2. the reflexive / strange-loop beat (forced new distinctions = the generative
   result; the structural form of a strange loop, scoped to the form, never
   experience; bounded by paper_upper_bound.md);
3. the closing maxim.
Added a half-clause naming the forced-distinctions result "the genuinely generative
result" so the beat foregrounds O4 (consistent with the abstract and contribution
(vii)) rather than the Hofstadter frame.

## Additivity
No claim or number altered; paragraph breaks + one clarifying half-clause. The
Tier-2 scoping and qualia disclaimer are unchanged.

## Gates
- Gate B: build green, all cites/refs/macros resolve, no bibtex warnings.
- No ledger change.

## Surfaced (NOT fixed here -- needs an authorial decision)
The level-up paragraph (§eval) says "all \ResLevelupTerrains{}=4 executable
instances", but the conclusion and §4 say "three" (11 occurrences) -- because the
level-up apparatus spans D (the deductive Rocq instance), which the paper never
introduces as an archetype. This 3-vs-4 inconsistency was introduced in circle 32.
Resolution options (author's call): (a) introduce D as a fourth executable instance
in §eval with a one-line parenthetical; or (b) rescope the level-up paragraph to the
three archetypes and present D's pool separately. Left for the author; the
conclusion itself is correct at "three".
