# 12-2330-paper-spec-25 — paper-impl loop, circle 25: polish the terrain archetypes section

STATUS: DONE
Editorial circle (no new evidence). §sec:archetypes (the three executable
instances A/B/C + synthesis) is data-heavy and macro-driven; it was polished
incrementally through circles 6-18 and reads cleanly. This pass audited it and
found one real defect.

## Audit
- Every reported number is a \Res* macro regenerated by a harness -- checked the
  prose reads correctly against the macros; no hand-typed figures.
- Cross-references all use \ref (Sections~\ref{sec:caseD}--\ref{sec:caseF}, the
  \S\ref forms in Tables 2-3); the table's compact \S\ref vs the prose's
  Section~\ref is the normal table-vs-prose convention -- kept.
- Scope caveats (constructed/deterministic/no matched-difficulty baseline) and the
  barrier-off honesty ("deterministic full-anchoring model ... a real-agent pilot
  finds the gap smaller") are accurate and consistent across A/B/C and synthesis.

## Change (one vocabulary fix)
- L556: "and a \emph{sentinel} runs the squeeze" -> "and the \emph{coordinator}
  runs the squeeze". "sentinel" appeared exactly ONCE in the whole paper, undefined,
  contradicting the canonical cast (Table~\ref{tab:cast}), where the role that runs
  the squeeze is the Coordinator (used 15x elsewhere). Same defect class as the
  circle-22 "engagement" drift: a one-off term breaking the established vocabulary.

## Additivity
No claim or number altered. Pure vocabulary alignment to the canonical cast.

## Gates
- Gate B: build green (18 pp), all cites/refs/macros resolve (no "??"), no bibtex
  warnings.
- grep: zero "sentinel" occurrences in paper.tex.
- No ledger change (prose-only).
