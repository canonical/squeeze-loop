# 12-5200-paper-spec-56 — paper-impl loop, circle 56: reframe synthesis + conclusion (review2 #1/#5)

STATUS: APPROVED
Third review2 circle. Apply the headline-result reframe (review2 #1) to the two
summary surfaces and demote skill (review2 #5) in the conclusion. Both already carry
partial caveats; make the relabel explicit so the 15/15 never reads as a measured
superiority and the 0 is clearly modeled-not-measured with the null adjacent.

## Changes (tex/paper.tex)
1. sec:synthesis opening: reword the detection sentence so 15/15 is framed as a
   \emph{consistency check} that the machinery is wired correctly, and the
   barrier-off figure is a deterministic full-anchoring \emph{model} (not a
   measurement) with the real-agent null adjacent ("finds no difference at this
   scale"). The structural-portability claim (one topology, four materials) stays.
2. sec:conclusion paragraph 1: relabel "showed it measurably: ... none with it off"
   as a consistency check + modeled baseline + measured-null; keep the
   underpowered-study disposition.
3. sec:conclusion paragraph 2: demote skill from "measurable ... skill bases enrich
   ... finding" to "a deterministic mechanism demonstration"; strange-loop sentence
   unchanged (Hofstadter decision still open).

## Gates
- Gate B: build green; all figures remain macro-bound (ResAll*); no new
  cite/number/macro/CLM; refs resolve; previously-SUPPORTED text byte-stable outside
  the three hunks.
- Gate C: ledger unchanged (CITE 46 + RESULT 28); reflexive macros reconcile;
  reflexive re-run byte-identical.

## Note
Safe direction throughout: relabels the demonstration, foregrounds the modeled
nature of the 0 and the measured null, and demotes skill to a mechanism
demonstration. No empirical claim strengthened; consistent with circles 54-55.
