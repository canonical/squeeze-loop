# 12-5600-paper-spec-60 — paper-impl loop, circle 60: compress the strange-loop apparatus (review2 #4)

STATUS: APPROVED
Acts on review2 #4 with the user's chosen middle path: COMPRESS the Hofstadter
apparatus in the body to one tight scoped paragraph. Keep the empirical core (the
\ResLoopCategories{} categories the loop generated while self-applying, and the
\ResLoopLevelCrossings{} logged episodes), keep the Tier-2 / structural-not-phenomenal
scope and the upper-bound bound; DROP the heaviest jargon -- "downward causation",
"level-crossing", "substrate", the persistence/return-to-origin mechanics, and the
video-on-monitor/quine rejection criteria.

## Changes (tex/paper.tex sec:reflexive)
- Replace the "A strange loop, scoped." paragraph with a ~half-length version:
  generativity (categories listed) + the logged self-modification episodes (plain
  language, no jargon) + the Hofstadterian crux + Tier-2 scope bounded by
  paper_upper_bound.md, with the why-only-Tier-2 hedge kept as a brief parenthetical
  (two conditions exhibited but not met). All three macros (ResLoopCategories,
  ResLoopLevelCrossings, ResLoopTier) remain in use -- no orphan, no generator change.

## Gates
- Gate B: build green; ResLoop* + ResReflexSpecDocs macros all still used (no orphan);
  no new cite/number/macro/CLM; refs resolve; previously-SUPPORTED text byte-stable
  outside the one paragraph.
- Gate C: ledger unchanged (CITE 46 + RESULT 28; CLM-073 OPN strange-loop still
  SUPPORTED-as-hedged); reflexive macros reconcile; reflexive re-run byte-identical;
  strange_loop.py unchanged (macros still emitted and used).

## Note
The empirical reflexive findings (8-defect self-audit earlier in the section; the
generativity categories) stay foregrounded; only the GEB scaffolding is trimmed.
Abstract and conclusion already carry a single scoped strange-loop sentence each
and are left as-is. CLM-073's hedge (Tier 2, structural not phenomenal) is preserved.
