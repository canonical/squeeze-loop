# 12-2100-paper-spec-20 — paper-impl loop, circle 20: polish the conclusion

STATUS: DONE
Editorial circle (no new evidence). Polish §sec:conclusion for accuracy and flow.

## Changes
1. STALE REFERENCE FIXED: "The compact statement, from the engagement notes, is
   the one we end on" -> "The compact statement is the one we end on". The field
   engagements were removed in circle 10; "the engagement notes" no longer exist
   in the paper, so the attribution dangled. (Same class of drift as the stale
   reflexive macros in circle 18: a residual left by an earlier removal.)
2. HONEST SCOPE ADDED: the conclusion ended purely on the deterministic result
   ("catching every seeded ... with the barrier on and none off"), out of step
   with the abstract and §sec:eval. Added one sentence stating what that result
   does NOT settle -- whether the gates shift outcomes against the natural error
   rate of strong models -- left as "a wired and runnable but so far underpowered
   study ... having declined to manufacture an effect the data did not show."
3. Flow: "demonstrate it measurably, catching" -> "showed it measurably: the
   gates catch"; "kept the topology" -> "kept this topology".

## Additivity
No new claim: the deterministic result is unchanged; the added scope sentence
restates §sec:eval (underpowered study, no-cherry-pick) and the abstract; the
removed phrase referred to deleted content. No number altered.

## Gates
- Gate B: build green (18 pp), all cites/refs/macros resolve, no bibtex warnings.
- grep: zero residual "engagement note" occurrences in paper.tex.
- No ledger change (prose-only).
