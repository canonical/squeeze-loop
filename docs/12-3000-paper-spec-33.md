# 12-3000-paper-spec-33 — paper-impl loop, circle 33: integrate U_self (strange-loop, scoped)

STATUS: DONE
Content circle. Integrates paper_upper_bound.md (U_self) and the strange-loop
self-application claim into the manuscript, scoped to the discharged tier, with
numbers that recompute and an OPN-typed honest scope.

## Why
The conclusion now frames the self-application as a Hofstadterian strange loop.
U_self caps that claim: structural, not phenomenal; Tier 2 today (O2/O3/O5 logged,
O1/O4 exhibited-not-discharged). This circle makes the claim earned-not-asserted.

## New evidence (O4 -- the genuine contribution)
- claims/category_ledger.tsv: 4 categories the loop GENERATED while self-applying
  and then re-applied to itself (hard/soft truth; richness=separable forks;
  diversity-restores-the-trial; gate-independence limit). This is the
  "squeezing forces new-concept creation" result.
- verify/strange_loop.py -> tex/macros/strangeloop.tex: ResLoopTier (2, the U_self
  §8 disposition), ResLoopCategories (4), ResLoopLevelCrossings (8, from
  manuscript_defects.tsv -- claims-about-the-method that forced substrate edits).
  Added to src/paper/_paperlib.py generators.

## Manuscript
- Reflexive section: new "A strange loop, scoped" paragraph -- the forced new
  distinctions (O4), the level-crossings (O2), persistence across circles
  (O5 via ResReflexSpecDocs), return-to-origin (O3), bounded by U_self to
  Tier 2 (the structural form of an "I", NOT consciousness/qualia), and explicitly
  strong enough to reject video-feedback / quine / self-hosting-compiler (N1/N2).
- Conclusion: scoped strange-loop sentence -- the squeeze forces new concepts and
  the self-applying loop instantiates the structural form of a strange loop; "we
  claim the shape, not the experience"; bound by paper_upper_bound.md.
- Cites hofstadter1979geb (already in bib); references U_self by path.

## Ledger
- CLM-072 RESULT (category_ledger.tsv, O4 evidence).
- CLM-073 OPN (strange-loop selfhood at Tier 2; Tier 1 pending O1/O4).
  (CLM-071 RESULT convergence_measures.py was added in parallel; my rows renumbered
  to avoid the id collision.)

## Gates
- Gate B: build green, all cites/refs/macros resolve, no bibtex warnings.
- Gate C: ledger RESULT (25) == regenerated ResReflexResultRows; CITE (46) == macro.
- Determinism: strange_loop.py + reflexive re-run byte-identical.

## Honest scope (loud-fail)
Admissible tier is 2 (structural), per U_self §8. Tier 0 (consciousness/experience)
is forbidden and asserted nowhere. The conclusion claims the shape of an "I", not
the experience.
